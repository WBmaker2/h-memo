{
  "name": "@h-memo/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@h-memo/memo-core": "1.0.0",
    "@h-memo/memo-ui": "1.0.0",
    "@h-memo/memo-sync": "1.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
